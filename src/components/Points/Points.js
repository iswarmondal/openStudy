const Points = ({textContent}) => {
    return (
        <div style={{ "width": "auto", "fontSize": "25px", "color": "white", "textAlign": "center" }}>{textContent}</div>
    )
}

export default Points;